@page
@model IndexModel
@{
    ViewData["Title"] = "홈";
}

<div class="hero min-h-screen bg-base-200">
    <div class="hero-content text-center">
        <div class="max-w-md">
            <h1 class="text-5xl font-bold">teledrop</h1>
            <p class="py-6">재작성 진행 중. 설계는 <code class="kbd kbd-sm">docs/DESIGN.md</code> 참고.</p>
            <form method="post" asp-page-handler="Logout">
                <button type="submit" class="btn btn-primary">로그아웃</button>
            </form>
        </div>
    </div>
</div>
